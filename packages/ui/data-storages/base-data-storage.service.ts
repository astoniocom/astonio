import {Injectable} from '@angular/core';

export class BaseDataStorageService {
  get(key:string, _default?:any):any {
  }

  set(key:string, value:any) {
  }

  clear() {
  }

  remove(key:string) {
  }
}